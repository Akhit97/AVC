package pages;

import basepage.BasePage;
import org.openqa.selenium.WebDriver;

public class P08TagScreen extends BasePage {
    public P08TagScreen(WebDriver driver) {
        super(driver);
    }
}
